# Set to true to require HTTP Basic authentication for the web interface.
REQUIRE_LOGIN=false
LOGIN_USERNAME=log
LOGIN_PASSWORD=replace-with-a-strong-password

# Required for administrator changes such as clearing the log.
ADMIN_PASSWORD=replace-with-a-strong-password

# Required to enable the /dump database export endpoint.
DUMP_TOKEN=replace-with-a-long-random-token
