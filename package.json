{
  "name": "radio-contest-logger",
  "version": "2.0.0",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "node --check server/server.js && node --check public/app.js && node --check public/bulk-entry.js && node --test"
  },
  "dependencies": {
    "basic-auth": "^2.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.18.2",
    "socket.io": "^4.8.1",
    "sqlite3": "^5.1.6"
  }
}
